This is the gh-pages branch used to host the portfolio page.

## Instructions to Upload React project:

git add .
git commit -m "commit"
git push

And now finally deploy the application using the following command in the terminal:

npm run deploy
